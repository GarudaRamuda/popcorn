title = "";

description = `
`;

characters = [];

options = {
  theme: "pixel",
  isPlayingBgm: true,
  isReplayEnabled: true,
};

function update() {  
  if (!ticks) {}
}

addEventListener("load", onLoad);